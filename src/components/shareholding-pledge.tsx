export function ShareholdingPledge({ symbol: _symbol }: { symbol: string }) {
  // Deprecated duplicate ownership/pledge section removed from the stock page.
  // The current stock view should not render the old Shareholding/Pledge block.
  return null;
}
