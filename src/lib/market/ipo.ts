import { createServerFn } from "@tanstack/react-start";
import { z } from "zod";

type Ipo={id:string;name:string;type:"Mainboard"|"SME";openDate:string|null;closeDate:string|null;issueSize:number|null;minSubscription:number|null;subscription:number|null;subscriptionSource:string|null;gmpPct:number|null;gmpSources:{source:string;pct:number|null}[];city:string|null;state:string|null;business:string|null;countries:{country:string;business:string;salesPct:number|null}[];profits:{year:string;value:number|null}[];priceBand:string|null;lotSize:number|null;verifiedAt:string};
const GROWW_SUBSCRIPTION="https://groww.in/ipo/subscription";
const MONEYCONTROL_OPEN="https://www.moneycontrol.com/ipo/open-ipos/";
const MONEYCONTROL_ROOT="https://www.moneycontrol.com/ipo/";
const cache=new Map<string,{exp:number;value:Ipo[]}>();
const UA="Mozilla/5.0 (compatible; Artha-market/1.0)";
async function getHtml(url:string,timeoutMs=12000){const c=new AbortController();const timer=setTimeout(()=>c.abort(),timeoutMs);try{const r=await fetch(url,{headers:{"User-Agent":UA,Accept:"text/html,application/xhtml+xml,application/json","Accept-Language":"en-US,en;q=0.9"},cache:"no-store",signal:c.signal});if(!r.ok)throw new Error(`HTTP ${r.status}`);return await r.text();}finally{clearTimeout(timer);}}
function clean(h:string){return h.replace(/<script[\s\S]*?<\/script>/gi," ").replace(/<style[\s\S]*?<\/style>/gi," ").replace(/<[^>]+>/g," ").replace(/&nbsp;/gi," ").replace(/&amp;/gi,"&").replace(/&#8377;|&#x20b9;/gi,"₹").replace(/&ndash;|&mdash;/gi,"-").replace(/&#39;|&apos;/gi,"'").replace(/&quot;/gi,'"').replace(/\s+/g," ").trim();}
function norm(s:string){return s.toLowerCase().replace(/&amp;/g,"and").replace(/limited|ltd\.?|private|pvt\.?|ipo|inc\.?/g,"").replace(/[^a-z0-9]+/g," ").trim();}
function num(v:unknown){const n=Number(String(v??"").replace(/,/g,""));return Number.isFinite(n)?n:null;}
function idFor(name:string){return norm(name).replace(/\s+/g,"-");}
function parseDate(s:string){let m=s.match(/(\d{1,2})\s+([A-Za-z]{3})\s+(\d{4})/);if(!m)m=s.match(/([A-Za-z]{3,9})\s+(\d{1,2}),?\s+(\d{4})/);if(!m)return null;const months:Record<string,string>={jan:"01",feb:"02",mar:"03",apr:"04",may:"05",jun:"06",jul:"07",aug:"08",sep:"09",oct:"10",nov:"11",dec:"12"};const firstDay=/^\d/.test(m[1]);const mo=months[(firstDay?m[2]:m[1]).slice(0,3).toLowerCase()];const day=firstDay?m[1]:m[2];return mo?`${m[3]}-${mo}-${String(Number(day)).padStart(2,"0")}`:null;}
function parsePriceBand(s:string){const m=s.match(/₹?\s*([\d,.]+)\s*(?:-|–|to)\s*₹?\s*([\d,.]+)/i);return m?`₹${m[1]} – ₹${m[2]}`:s.match(/₹\s*([\d,.]+)/)?.[0]??null;}
function priceNumbers(s:string){const m=s.match(/₹?\s*([\d,.]+)\s*(?:-|–|to)\s*₹?\s*([\d,.]+)/i);return m?[num(m[1]),num(m[2])]:[null,null];}
function extractNumber(t:string,patterns:RegExp[]){for(const p of patterns){const m=t.match(p);if(m){const n=num(m[1]);if(n!=null)return n;}}return null;}
function blank(name:string,type:"SME"|"Mainboard"="Mainboard"):Ipo{return {id:idFor(name),name,type,openDate:null,closeDate:null,issueSize:null,minSubscription:null,subscription:null,subscriptionSource:null,gmpPct:null,gmpSources:[],city:null,state:null,business:null,countries:[],profits:[],priceBand:null,lotSize:null,verifiedAt:new Date().toISOString()};}
function parseRows(html:string):Ipo[]{const rows=[...html.matchAll(/<tr\b[\s\S]*?<\/tr>/gi)].map(m=>clean(m[0]));const out:Ipo[]=[];for(const row of rows){const dates=[...row.matchAll(/\d{1,2}\s+[A-Za-z]{3,9}\s+\d{4}/g)].map(m=>m[0]);if(!dates.length)continue;const type:"SME"|"Mainboard"=/SME/i.test(row)?"SME":"Mainboard";const before=row.split(/\d{1,2}\s+[A-Za-z]{3,9}\s+\d{4}/)[0];const name=before.replace(/Image:\s*/gi,"").replace(/Company Name|Type|Open Date|Close Date|Issue Size|Issue Price/gi," ").replace(/\b(Mainboard|SME)\b/gi," ").replace(/\s+/g," ").trim();if(!name||name.length<2||/company name/i.test(name))continue;const ipo=blank(name,type);ipo.closeDate=parseDate(dates[0]);const after=row.slice(row.indexOf(dates[0])+dates[0].length);ipo.priceBand=parsePriceBand(after);ipo.lotSize=extractNumber(after,[/lot\s*size\s*(\d[\d,]*)/i]);const cr=after.match(/₹?\s*([\d,.]+)\s*(?:Cr|crore)/i);ipo.issueSize=cr?num(cr[1]):null;const prices=priceNumbers(ipo.priceBand??after);if(ipo.lotSize&&prices[0]!=null)ipo.minSubscription=prices[0]*ipo.lotSize;out.push(ipo);}return out;}
function parseSubscriptionRows(html:string):Ipo[]{const rows=[...html.matchAll(/<tr\b[\s\S]*?<\/tr>/gi)].map(m=>clean(m[0]));const out:Ipo[]=[];for(const row of rows){const dates=[...row.matchAll(/\d{1,2}\s+[A-Za-z]{3,9}\s+\d{4}/g)].map(m=>m[0]);if(!dates.length)continue;const type:"SME"|"Mainboard"=/SME/i.test(row)?"SME":"Mainboard";const before=row.split(/\d{1,2}\s+[A-Za-z]{3,9}\s+\d{4}/)[0];const name=before.replace(/Image:\s*/gi,"").replace(/Company Name|Type|Close Date|Issue Size|Issue Price|QIB|NII|Retail|Employee|Total/gi," ").replace(/\b(Mainboard|SME)\b/gi," ").replace(/\s+/g," ").trim();if(!name||name.length<2||/company name/i.test(name))continue;const ipo=blank(name,type);ipo.closeDate=parseDate(dates[0]);const after=row.slice(row.indexOf(dates[0])+dates[0].length);ipo.priceBand=parsePriceBand(after);const cr=after.match(/([\d,.]+)\s*Cr/i);ipo.issueSize=cr?num(cr[1]):null;const total=[...after.matchAll(/(\d+(?:\.\d+)?)\s*x\b/gi)].map(m=>num(m[1])).filter((v):v is number=>v!=null);if(total.length)ipo.subscription=total[total.length-1];const prices=priceNumbers(ipo.priceBand??after);const lot=extractNumber(after,[/lot\s*size\s*(\d[\d,]*)/i]);if(lot)ipo.lotSize=lot;if(ipo.lotSize&&prices[0]!=null)ipo.minSubscription=prices[0]*lot;out.push(ipo);}return out;}
function extractGmp(text:string){const m=clean(text).match(/(?:GMP Today|Current GMP Status|Live GMP|Current GMP|GMP)[\s\S]{0,180}?₹\s*(-?[\d,]+(?:\.\d+)?)/i);return m?num(m[1]):null;}
async function enrichGmpFromGroww(ipo:Ipo){try{const html=await getHtml(`https://groww.in/ipo/${ipo.id}`,8000);const g=extractGmp(html);if(g!=null){ipo.gmpSources=[{source:"Groww",pct:g}];ipo.gmpPct=g;}}catch{ipo.gmpSources=[];ipo.gmpPct=null;}}
async function enrichMoneycontrol(ipo:Ipo){try{const t=clean(await getHtml(`${MONEYCONTROL_ROOT}${ipo.id}-ipo`,8000));if(t.length<100)return;const m=t.match(/IPO Details[\s\S]{0,5000}/i)?.[0]??t;ipo.priceBand=ipo.priceBand??parsePriceBand(m);ipo.lotSize=ipo.lotSize??extractNumber(m,[/Lot Size\s*(\d[\d,]*)/i,/Lot size[^\d]{0,30}(\d[\d,]*)/i]);ipo.issueSize=ipo.issueSize??extractNumber(m,[/Issue Size[^\d]{0,50}₹?\s*([\d,.]+)\s*(?:Cr|crore)/i]);const about=t.match(/(?:Company Info|Business|About)[\s\S]{0,2500}/i);if(about)ipo.business=about[0].slice(0,1600);const city=t.match(/\b(Mumbai|Delhi|Bengaluru|Bangalore|Chennai|Pune|Ahmedabad|Kolkata|Hyderabad|Jaipur|Surat|Noida|Gurugram|Gurgaon|Vadodara|Indore|Rajkot)\b/i);if(city)ipo.city=city[1];}catch{}}
function todayIST(){return new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Kolkata",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date());}
async function loadIpos(){const hit=cache.get("open");if(hit&&hit.exp>Date.now())return hit.value;const [groww,mc]=await Promise.allSettled([getHtml(GROWW_SUBSCRIPTION),getHtml(MONEYCONTROL_OPEN)]);const growwIpos=groww.status==="fulfilled"?parseSubscriptionRows(groww.value):[];const mcIpos=mc.status==="fulfilled"?parseRows(mc.value):[];const byCompany=new Map<string,Ipo>();for(const ipo of [...growwIpos,...mcIpos]){const key=norm(ipo.name);const old=byCompany.get(key);if(!old)byCompany.set(key,ipo);else{old.type=old.type==="SME"||ipo.type==="SME"?"SME":"Mainboard";old.closeDate=old.closeDate??ipo.closeDate;old.openDate=old.openDate??ipo.openDate;old.issueSize=old.issueSize??ipo.issueSize;old.priceBand=old.priceBand??ipo.priceBand;old.lotSize=old.lotSize??ipo.lotSize;old.minSubscription=old.minSubscription??ipo.minSubscription;old.subscription=old.subscription??ipo.subscription;}}const today=todayIST();const ipos=[...byCompany.values()].filter(x=>x.closeDate==null||x.closeDate>=today);const enriched=await Promise.all(ipos.map(async ipo=>{if(ipo.subscription!=null)ipo.subscriptionSource="Groww verified";await enrichMoneycontrol(ipo);await enrichGmpFromGroww(ipo);ipo.verifiedAt=new Date().toISOString();return ipo;}));enriched.sort((a,b)=>{const ad=a.closeDate??"9999-99-99",bd=b.closeDate??"9999-99-99";return ad===bd?a.name.localeCompare(b.name):ad.localeCompare(bd);});cache.set("open",{exp:Date.now()+30000,value:enriched});return enriched;}
export const fetchOpenIpos=createServerFn({method:"POST"}).validator((data:unknown)=>z.object({refresh:z.boolean().optional()}).parse(data)).handler(async()=>{try{return await loadIpos();}catch{return[];}});
